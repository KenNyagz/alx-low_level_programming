#include "main.h"
#include <stdio.h>

/**
*main -entry point
*function prints its name
*@argc:  argument count
*@argv: argument array
*Return: (0)
*/

int main(int argc, char *argv[])
{
int i;
while (argv[i] = 0)
for (j = 0; j < strlen(argv); j++)
_putchar(argv[j]);
return (0);

}
