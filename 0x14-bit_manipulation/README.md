Now into bit manipulation.

Manipulating data an a low level.
Using bitwise operators:
-bitwise `and` (&)
-bitwise `or` (|)
-bitwise `not` (~)
and finally `xor` (^)

Concepts revolve around binary and hexadecimal(and decimal)
